{
  "name": "betweb-monorepo",
  "private": true,
  "workspaces": [
    "frontend",
    "user-frontend"
  ],
  "scripts": {
    "admin:dev": "npm run dev --prefix frontend",
    "admin:build": "npm run build --prefix frontend",
    "user:dev": "npm run dev --prefix user-frontend",
    "user:build": "npm run build --prefix user-frontend"
  }
}
